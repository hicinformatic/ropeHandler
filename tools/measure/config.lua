measureConfig = {
    distance = 6000,
    controls = {
        37, -- tab
        24, -- leftClick
        257, -- leftClick
        25, -- rightClick
        96, -- scrollUp
        97, -- scrollDown
    }
}

measureConfig.items = {
    scanner = {
        model = "WEAPON_DIGISCANNER", --WEAPON_HACKINGDEVICE
    },
}

measureConfig.animations = {

}

measureConfig.crosshair = {
    showLine = true,
    colorLine = { 255, 0, 0, 255 },
    crosshairDict = "helicopterhud",
    crosshaireName = "hud_dest",
    colorOff = {128, 128, 128, 125},
    colorOn = {255, 255, 255, 255},
}
